---
- hosts: group1

  tasks:
    - ping: ~

    - name: Update APT package manager repository cache
      become: true
      apt:
        update_cache: yes

    - name: Upgrade the existing packages
      become: true
      apt:
        upgrade: dist

    - name: install cowsay - its probably
      apt:
        name: cowsay
      become: true
